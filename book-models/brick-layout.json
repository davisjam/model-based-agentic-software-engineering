{
  "_note": "Declared brick-layout constraints for the Appendix-C mechanism-catalog brick grid (appendix-restructure §14). Consumed at build time by book/build_book.py:_load_brick_layout(). Each key is a catalogue entry slug; `pair_with` places the named sibling immediately adjacent; `near` keeps it sequentially close. RE-TUNED in restructure sub-wave 5b: `span` is DROPPED entirely — every brick is span-1. The render-based vision-judge review (book/_design/drafts/appendix-c-vision-judge-260804.md) found span-2 unjustified: a span-2 brick renders its diagram centered at native small size in a wide grey field, so the extra width benefits only the summary's line-count, not the figure. A uniform span-1 grid packs cleaner and removes the tiny-diagram-in-grey look. Only adjacency hints remain: the test/build-serializer sibling pair and the composed/symbol near-adjacency (both GLYPH Bridge graph/trace models).",
  "bricks": {
    "composed-state-machine-model": { "near": "symbol-anchored-traceability-graph" },
    "test-serializer": { "pair_with": "build-serializer" }
  }
}
