1. Is “one node per registered remediation pass” an intentional permanent granularity rule, or merely the current projection mechanism? If a pass contains multiple independently consequential computations with different complexity, determinism, cost, or evidence properties, should those remain attributes of the pass-level node, or can they become sub-pass nodes? What is the rule for when that decomposition is warranted?
2. What is the intended path from the current two-edge seed to a substantially complete dependency DAG? Are edges ultimately expected to be manually authored and ratcheted, projected from typed pass composition, inferred from instrumentation, or populated through some combination? In particular, is there a plausible route to making edge alignment mechanically strong in the way node alignment already is?
3. Is INV-GRAPH-DECL-INERT intended as a permanent architectural invariant, or does it describe the model’s present authority level? Could selected parts of the model eventually become authoritative—for example, generating configuration selection or pipeline composition—or is the intended architecture permanently runtime-as-territory with the graph restricted to declaration, measurement, and audit?
4. Why a static graph and not using this as the executable pipeline? What aspects are models that are above the code, and not suitable for making explicitly code? How do you ensure alignment?
