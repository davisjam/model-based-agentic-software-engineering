the fleet that produces the work, and the substrate that runs it — how agents are dispatched, isolated, gated, and observed.
the structured models the fleet reasons through — the shared map a bounded agent uses to operate a codebase larger than its context.
the shipped artifact itself — its canonical seams, its content-fidelity validation, and its conformance controls.
