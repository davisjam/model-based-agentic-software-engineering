<!-- part-title: Alignment -->
<!-- chapter-title: Governing the Governance -->

# Governing the Governance

<!-- gloss-only: Governance graph | A structured model whose nodes are the fleet's own governance mechanisms (each tagged by the event it fires on and the shared resources it reads, writes, or locks) and whose edges are the conflicts between them over a shared resource, in a closed taxonomy of contradiction, contention, ordering, and precedence; the dynamic dual of a static catalogue of controls, holding the interaction dimension a list omits. -->

<!-- point: every-control-reasonable-alone-the-system-of-controls-still-wrong | Every control can be reasonable in isolation and the system of controls still be wrong. | terms: governance-graph, constraint -->
Every control in an environment can be reasonable in isolation and the system of controls can still be
wrong. One mechanism requires a squash; another interprets the resulting large diff as unauthorized scope
expansion. Two hooks fire on the same lifecycle event with no declared order. A validator survives long
after the obligation that justified it disappeared. A newly important obligation has no control at all.
None of these failures is visible by reading one mechanism's code.

<!-- point: successful-environments-accumulate-machinery-so-the-estate-is-a-portfolio-to-manage | Successful environments accumulate machinery; that estate becomes an engineering object — a portfolio of capital assets to manage. | terms: governance-graph, engineering-capital -->
Governance conversion creates this problem deliberately: successful environments accumulate machinery.
That machinery becomes an engineering object in its own right. If the durable controls are engineering
capital, the accumulated estate is a **portfolio** — and a portfolio has to be managed. Which assets still
produce value, which have depreciated, which liabilities have appeared between them, and where has nothing
been invested at all? MAGE asks three questions of the estate: **what controls exist, how do they
interact, and which obligations do they cover?** The answers are three views over one control estate — a
catalogue, an interaction graph, and a coverage relation.

<!-- point: alignment-turned-on-itself-a-model-of-the-controls-made-binding-on-the-controls | Alignment turned on itself: a model of the controls, made binding on the controls. | terms: governance-graph -->
This is Alignment turned on itself. Once the control estate becomes consequential, represent it explicitly
and hold it to its own obligations.

## Catalogue the controls

<!-- section-terms: governance-graph -->

<!-- point: the-catalogue-gives-each-control-a-stable-identity | Catalogue each control: a stable identity, the boundary it acts at, the resources it touches, the obligation it serves, and its authority role. | terms: governance-graph, shared-resource -->
Start with the nodes. Each governance mechanism gets a stable identity, the event or boundary at which it
acts, the resources it reads or changes, the obligation or failure class that justifies it, and its
authority role or roles. The catalogue is simple. Its value is that the next two views can reason over
stable identities rather than rediscovering controls from scripts, hooks, CI files, and institutional
memory.

<!-- point: a-catalogue-alone-cannot-show-conflict-or-a-missing-control | A catalogue alone cannot tell you whether two sensible controls conflict, nor whether an important obligation has no control; those are relational questions. | terms: governance-graph, shared-resource -->
A catalogue alone cannot tell you whether two individually sensible controls conflict, nor whether an
important obligation has no control. Those are relational questions. The first lives on the edges between
controls.

## The conflict is the edge

<!-- section-terms: governance-graph, shared-resource -->

<!-- point: each-collision-lives-in-the-space-between-two-controls-drawn-nowhere | A collision lives between two controls, drawn nowhere until the graph draws it. | terms: shared-resource, governance-graph -->
Two collisions from the case study make it concrete. In the first, one mechanism required several commits
collapsed into a single squashed one, while another read the resulting combined diff as unauthorized scope
expansion and flagged it — two controls, one commit-set, contradictory demands, caught only when a real
commit tripped both. In the second, two hooks fired on the same turn-end with no order declared between
them, each able to block, and the pipeline simply hoped they composed. Neither collision lives in any one
control's code. Each lives in the space *between* two controls, and that space was drawn nowhere — until
[ref:collision-edge] draws it.

<!-- label: collision-edge -->
<!-- figure: assets/collision-edge.svg | *The Conflict Is the Edge.* Each control is reasonable alone; the contradiction appears only when both act on the same commit-set. Interaction failures belong to the relation between controls, not necessarily to either control's implementation. -->

<!-- point: the-interaction-graph-joins-controls-by-shared-resource | The interaction graph uses controls as nodes and shared resources as the principal join key; controls need not call one another to collide. | terms: governance-graph, shared-resource -->
The interaction graph uses controls as nodes and shared resources as the principal join key. A resource
may be ordinary — a lock, a file, a commit-set, a deployment slot — or operational, such as a lifecycle
event or a context budget. Controls need not call one another to interact; they only need to make
potentially incompatible demands on the same modeled resource.

<!-- point: docable-implemented-governance-graph-is-intentionally-partial | DocAble's implemented governance graph is intentionally partial — a small high-value slice of a larger estate, not comprehensive coverage. | terms: governance-graph, shared-resource -->
DocAble's implemented governance graph is intentionally partial. It models a small, high-value slice of a
much larger control estate, including interactions around the orchestrator lifecycle. The point is not
comprehensive coverage. It is to make recurring interaction questions explicit enough to query where
experience has shown they matter.

<!-- point: shared-resource-is-a-useful-conservative-relation-not-a-proof-of-completeness | Shared resource is a useful conservative relation, not a proof that the graph captured every interaction. | terms: shared-resource, semantic-gap -->
Shared resource is a conservative interaction relation, not proof that every conflict has been captured.
Within the represented surface, it lets the environment ask useful questions before
deployment: which controls fire on this event, which touch this resource, which pairs have no declared
order, and which known combinations impose incompatible requirements.

<!-- point: decidable-edges-a-lint-settles-semantic-ones-a-validator-or-human-judges | Some candidate conflicts are mechanically decidable; others are semantic — a sensor produces evidence of the interaction, a validator or human judges whether it is a conflict. | terms: governance-graph, semantic-gap -->
Some candidate conflicts are mechanically decidable: a lock cycle, two exclusive writers, or two same-slot
mechanisms with no permitted ordering. Others are semantic. A static check can **sense** that two controls
touch the same commit-set; a validator — or a human reviewer — must decide whether their requirements
actually conflict. Again the four roles matter: producing evidence of an interaction is not the same as
judging the interaction invalid. This is where the motivating commit-set collision was really lost: the
machine could flag that two controls touched the commit-set, but only judgment could call the
contradiction. The graph makes both the flag and the prompt land *before* the control ships.

<!-- point: the-graph-makes-mechanisms-compose-explicit-and-checkable | A governed environment is a claim that its mechanisms compose; the graph makes that claim explicit and checkable at authoring time. | terms: governance-graph -->
A governed environment is not just a pile of mechanisms; it is a claim that those mechanisms compose. The
interaction graph makes that claim explicit enough to inspect before collision: which controls fire on
this event, which touch this resource, and which combinations require review?

## Coverage: obligations against mechanisms

<!-- section-terms: governance-graph, governance-conversion -->

<!-- point: a-catalogue-and-a-graph-do-not-say-what-should-be-governed-but-is-not | A catalogue says what exists and a graph says where controls collide; neither says what should be governed but is not. | terms: governance-graph, governance-conversion -->
Interaction is only half of meta-governance. A catalogue tells you what controls exist; an interaction
graph tells you where controls may collide. Neither tells you what *should* be governed but is not. For
that, join the mechanisms to the obligations or failure classes that justify them.

<!-- point: coverage-is-a-requirements-traceability-problem-gap-vs-orphan | Coverage is a small requirements-traceability problem: an obligation with no adequate mechanism is under-governance; a mechanism with no obligation is an orphan. | terms: governance-graph, governance-conversion -->
The result is a small requirements-traceability problem. An important obligation with no adequate mechanism
is **under-governance**. A mechanism with no current obligation, failure class, or purpose is an
**orphan** — candidate governance debt. The useful question is not "how many controls do we have?" It is
whether consequential obligations have appropriate coverage and whether every surviving control can still
explain why it exists. [ref:census-trio] sets the three views side by side over one control estate.

<!-- label: census-trio -->
<!-- figure: assets/census-trio.svg | *Three Views of One Control Estate.* The catalogue identifies the controls; the interaction graph exposes relations among them over a shared resource; the coverage relation joins controls to the obligations that justify them. The same underlying identities support three different engineering questions. One estate, three views: the catalogue names the controls, the graph adds their interactions over shared resources, and coverage adds the obligations that justify them. In coverage, an obligation with no control is a GAP; a control with no obligation is an ORPHAN. -->

<!-- point: gap-is-under-governance-orphan-is-governance-debt-neither-automatically-bad | A gap is under-governance; an orphan is governance debt. Neither is automatically bad — an orphan is a finding to reconcile, not proof to delete. | terms: governance-graph -->
Two definitions do the diagnostic work, and neither verdict is automatic.

- **Gap / under-governance.** A consequential obligation for which no adequate mechanism carries the
  intended evidence or authority. A gap is a place the environment is silent where it should speak.
- **Orphan / governance debt.** A mechanism that no longer traces to a current obligation, failure class,
  or documented purpose. An orphan is a finding to reconcile, not proof the mechanism should be deleted.

## Grow, reconcile, retire

<!-- section-terms: governance-graph, teetering-tower, engineering-capital -->

<!-- point: conversion-is-reactive-coverage-is-proactive-a-healthy-environment-needs-both | Governance conversion is reactive and coverage auditing is proactive; a healthy environment needs both. | terms: governance-graph, governance-conversion -->
Governance conversion and coverage auditing pull in opposite directions, and a healthy environment needs
both.
Conversion is reactive: failures reveal gaps and add durable structure where experience has shown it
matters. Coverage auditing is proactive: it asks about important obligations that have not yet failed
loudly enough to attract a control. One grows the estate from experience; the other checks whether that
growth has become lopsided.

<!-- point: the-same-audit-pushes-against-permanent-accumulation-capital-is-not-the-number-of-controls | The same audit pushes against permanent accumulation: engineering capital is not the number of controls. | terms: engineering-capital, teetering-tower -->
The same audit also pushes against permanent accumulation. A control whose purpose has disappeared should
be reconciled or retired; two controls that now overlap may be consolidated; a mechanism whose maintenance
cost exceeds the failure it prevents may no longer be worth carrying. **Engineering capital is not the
number of controls.** Capital that no longer produces value becomes overhead — the depreciation the last
chapter named, now managed rather than merely observed.

<!-- point: the-meta-model-stays-connected-where-controls-are-modeled-explicitly | Where DocAble models controls explicitly, entries can be anchored to their implementations and drift-checked, so a mis-placed control fails loud. | terms: traceability, teetering-tower -->
The control model must remain connected to the controls it represents. Where DocAble models controls
explicitly, entries can be anchored to their implementations and checked for drift, so a control the rule
cannot place fails loud rather than dropping into a silent default bucket. That is one implementation of the
broader rule Part II already established: if the organization is going to reason through a model of the
control estate, the model must stay connected to the territory it claims to describe.

<!-- point: stop-the-recursion-unless-experience-earns-more-model-the-estate-that-exists | Stop the recursion unless experience earns more: a governance model can itself become bureaucracy. | terms: governance-graph, teetering-tower -->
Stop the recursion there unless experience gives you a reason to go further. A governance model can become
another source of bureaucracy if every possible interaction generates another speculative control. Model
the estate that exists, represent the conflict classes the system has reason to care about, and add
machinery only when evidence earns it.

The external cases examined for Part V do not provide a comparable control-estate model.[^estate]

[^estate]: Among the external systems reconstructed for Part V, I did not find a reported analogue of
this control-estate model; this element therefore remains grounded primarily in the case at hand.

<!-- worked-examples: governs-accumulated-governance -->
### Example — Two safety rules {other}
A building's fire system unlocks doors when it detects smoke; its security system may lock those same
doors under an intrusion policy. Each rule is sensible in isolation while the pair demands incompatible
states of the same resource. The contradiction belongs to the interaction, not to either rule's own
logic.

### Example — DocAble
DocAble models its governance mechanisms as an estate rather than only as individual scripts. Controls are
anchored to their implementations, interaction relations identify shared resources, and coverage traces
controls back to the obligations or failures that justify them. The point is not that every possible
conflict becomes deterministic. It is that the control system exposes enough structure to ask, before the
next collision, which mechanisms interact and which important obligations have no adequate coverage.

<!-- takeaway -->
Governance conversion accumulates durable judgment; meta-governance keeps that accumulation coherent.
Catalogue the controls, expose their interactions, trace them to the obligations they serve, and retire
machinery that no longer earns its cost.
<!-- worked-examples-end -->

<!-- point: part-3-treated-alignment-as-a-separate-discipline-part-2-enlarges-the-scale | Part III treated Alignment as a separate engineering discipline; authority can act directly, and Part II matters because richer representations enlarge the scale at which it can operate. | -->
Part III has treated Alignment as a separate engineering discipline. Authority can act directly on actions
and artifacts through permissions, types, APIs, and local gates; it does not require a comprehensive system
model. Part II matters because richer representations enlarge the semantic scale at which that authority
can operate. Once architecture, ownership, behavior, measurement, or policy is explicit, the environment
can govern properties that would otherwise require an agent or a human to reconstruct their meaning each
time.

<!-- point: the-part-developed-the-discipline-in-five-moves | The Part developed the discipline in five moves: boundary, obligation, roles, conversion, meta-governance. | -->
The Part developed the discipline in five moves: locate the boundary where authority can act; state an
independent obligation and place its mechanism where the obligation is legible; decompose authority into
constraint, sensor, validator, and gate; convert recurring failures into durable representation or
control; then treat the accumulated control estate as a system in its own right.

<!-- point: modeling-and-alignment-are-both-on-the-table-but-that-is-not-yet-a-method | Modeling and Alignment are both on the table, but that is not yet a method; Part IV joins the two into practice. | -->
Modeling and Alignment are now both on the table. That still is not a method. Someone still has to decide
when a problem deserves a model, when an obligation deserves authority, how work should move through the
environment, and what to do when either thesis runs out. **Part IV joins the two into practice.**
