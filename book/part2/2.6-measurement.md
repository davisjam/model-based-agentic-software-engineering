<!-- part-title: Modeling -->
<!-- chapter-title: Measurement Models: How Much, and Against What Bound? -->

# Measurement Models: How Much, and Against What Bound?

Every system emits numbers, and most of them are just telemetry: observations with no engineering
meaning attached. A measurement model gives selected observations meaning by *relating* them: usage to
cost, cost to a budget, concurrency to a capacity. It answers the quantitative question the earlier
families set aside — how much, and against what bound?

## The Canonical Move: Quantities, Relations, and Bounds

<!-- point: quantitative-models-relate-quantities-so-a-bound-becomes-statable | Performance, capacity, and cost models relate quantities to budgets and envelopes; the move is small but decisive — telemetry is observation, a measurement model relates quantities so that a bound becomes statable. -->
Engineering has many quantitative representations: performance and parametric models, capacity models,
cost models, and the budgets and envelopes they are compared against; queueing and resource models
appear where contention matters. The move they share is small but decisive.
Telemetry is observation; a measurement *model* relates quantities so that a bound becomes statable. A
node is a quantity; a relation is an accounting or functional link between quantities — usage times
price is cost, concurrent jobs are capacity demand; a bound is the envelope a quantity is compared
against ([ref:canonical-measurement]).

<!-- label: canonical-measurement -->
<!-- figure: assets/canonical-measurement.svg | *The measurement move: quantities against a bound.* Telemetry becomes a model when quantities are related and a value is read against a declared envelope; the analyses aggregate and compare rather than watch a stream call by call. -->

<!-- point: a-measurement-model-turns-are-we-within-budget-into-a-comparison | Once quantities are related to a bound, upper and lower bounds, budgets, capacities, rates, tolerances, and margins become expressible, and "are we within budget?" becomes a comparison against a modeled bound rather than a guess over a dashboard. -->
Once quantities are related to a bound, the claims a designer wants become expressible: upper and lower
bounds, budgets, capacity constraints, rates, conservation relations, tolerances, and margins. The
analyses follow — aggregation, comparison against a bound, sensitivity and capacity analysis,
forecasting. [ref:measured-quantity-comparison] draws the core relation: a current value read against a
separately declared reference. The payoff is that "are we within budget?" becomes a comparison against a
modeled bound rather than a guess over a dashboard.

<!-- label: measured-quantity-comparison -->
<!-- figure: assets/measured-quantity-comparison.svg | *Core measurement relation.* A current value is interpreted against a separately declared reference bound. The model defines what is compared; it does not determine the consequence of the comparison. -->

## DocAble: GenAI Cost and Capacity

<!-- point: the-two-chains-are-separate | Usage and pricing yield an attributable cost read against a budget; concurrency yields a capacity demand read against an envelope; the two chains answer different questions and are compared against different bounds. -->
DocAble's consequential quantities are the cost of its GenAI usage and the capacity its concurrent work
demands. [ref:genai-cost-capacity-model] draws the two chains, kept deliberately separate because they
answer different questions and are compared against different bounds.

<!-- label: genai-cost-capacity-model -->
<!-- figure: assets/genai-cost-capacity-model.svg | *GenAI cost-and-capacity model.* Generation work fans into usage and concurrency; usage and pricing determine cost against a budget, and concurrency contributes to capacity demand against a capacity envelope. The model names the quantities worth relating — the interpretation placed over raw requests and charges, not the telemetry itself. -->

Measurement counts what is happening now. The last family asks what already happened, and what records
it.
