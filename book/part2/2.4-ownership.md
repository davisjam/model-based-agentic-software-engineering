<!-- part-title: Modeling -->
<!-- chapter-title: Ownership Models: Who Controls What, and When? -->

# Ownership Models: Who Controls What, and When?

Autonomous systems make runtime ownership unusually important. It is not enough to know which service
owns a responsibility. When several workers may encounter the same work, the system must also represent
who currently holds a claim, and when that claim ceases to matter. Ownership across time is its own
model class.

DocAble represents work that has been claimed but not yet completely merged using an **in-flight lease**.
At the model level the important facts are few: the work item, its current owner, an epoch that
distinguishes one claim from another, the lifetime of the lease, and release or reclamation.

> #### In-flight lease · Ownership
>
> - **Engineering question** — Who currently appears to own this work, and when may the system treat
>   that ownership as stale?
> - **Model** — a lease over a work item: an owner, an epoch, a lifetime, and the transitions claim →
>   release and expire → reclaimable.
> - **Property** — each claim has an identifiable owner, epoch, and lifetime; an expired claim can be
>   distinguished from a current one and reclaimed.
> - **Quality attribute** — liveness, crash-recovery, and fair progress under contention.

<!-- point: an-ownership-model-represents-a-claim-and-when-it-goes-stale | An in-flight lease represents who holds a claim, distinguishes one claim from the next by epoch, and says when the claim may be treated as stale. -->
[ref:in-flight-ownership-states] draws the resulting ownership model. A claim installs an owner, an
epoch, and a lifetime; a merge releases it; expiry makes the work reclaimable. The representation
suppresses the machinery that realizes ownership — it need not describe Redis operations, database
transactions, worker processes, broker delivery, or merge implementation to answer *who currently
appears to own this work, and when may the system treat that ownership as stale?*

<!-- label: in-flight-ownership-states -->
<!-- figure: assets/in-flight-ownership-states.svg | *In-flight ownership model.* A current claim records owner, epoch, and lifetime. Merge releases the claim; expiry makes it reclaimable. The model distinguishes a live claim from a stale one without representing the storage machinery that implements the lease. -->

<!-- point: recovery-is-not-permission | A lease recovers work whose owner has disappeared; it does not authorize a new claimant to repeat an already-authorized side effect. Durable compare-and-set underneath the lease protects that. -->
The model also exposes an important distinction. The lease helps the system represent active ownership
and recover work whose apparent owner has disappeared. It is *not* the final authority for performing a
consequential side effect. Durable compare-and-set logic underneath the lease protects that property. A
slow worker may therefore lose a lease and cause work to be redelivered without granting the new
claimant permission to repeat an already-authorized effect.

<!-- point: ownership-failures-emerge-from-interleavings | The difficult ownership failures emerge from interleavings, not from any single transition. -->
The difficult ownership failures do not necessarily appear in any one transition. They emerge from
interleavings. Suppose one worker holds epoch 7, appears stale, and is reclaimed; another worker receives
epoch 8; then the original worker wakes and attempts a late release using its old claim. Every individual
action may be locally reasonable. The engineering question is whether their composition can destroy the
current owner's lease or otherwise leave the system in an invalid state.

<!-- point: the-model-makes-the-interleaving-question-finite | The ownership model makes the interleaving question finite enough to ask explicitly. -->
The ownership model makes that question finite enough to ask explicitly: owner, epoch, lifecycle state,
reclaim, and release are the relevant variables and moves. The implementation may involve databases,
queues, Lua, workers, and timers, but those details need not all enter the model. The reduction exists so
that the interleavings carrying the ownership property can be explored directly.
[ref:lease-interleaving-trace] follows one such sequence to the fork an epoch fence closes.

<!-- label: lease-interleaving-trace -->
<!-- figure: assets/lease-interleaving-trace.svg | *Why ownership becomes an interleaving problem.* Reclaim and reissue can overlap a stale holder's late release. An epoch fence makes the old holder's release inapplicable to the current claim. -->

**Recovery is not permission.** That distinction will matter in Part III: the same represented state can
support different authority decisions.

<!-- point: model-classes-overlap-one-fact-serves-several-questions | The same in-flight lease answers an ownership question and a measurement question at once, because the classes sort engineering questions, not data structures. -->
The lease also shows that model classes overlap. Active leases can be counted to estimate demand, so the
same representation contributes to an ownership question — *who controls this work?* — and a measurement
question — *how much work is genuinely in flight?* [ref:in-flight-lease-overlap] draws that overlap: one
represented fact fanning out into ownership, behavioral, and measurement views. The categories in this
Part classify engineering questions, not mutually exclusive data structures.

<!-- label: in-flight-lease-overlap -->
<!-- figure: assets/in-flight-lease-overlap.svg | *Model classes overlap.* The same lease state supports ownership, behavioral, and measurement questions. The classes distinguish purpose, not storage objects. -->

The next chapter turns from *who holds the work* to *who is permitted to act* — the declared decisions a
system makes about which of its parts may reach which.
