<!-- part-title: The Evidence -->
<!-- chapter-title: From Failure to Engineering Capital -->

# From Failure to Engineering Capital

> ### This chapter illustrates {#illustrates}
>
> ✓ Governance Conversion · ✓ Engineering Capital · ✓ Residual Judgment · ✓ Capital Formation Without a Gate

<!-- point: governance-conversion-is-an-empirical-claim-about-durable-structure | Governance conversion claims some episodes leave durable structure later work inherits. | terms: governance-conversion, engineering-capital -->
Governance conversion makes an empirical claim: some engineering episodes leave behind durable
structure that later work inherits instead of paying for the same judgment again. This chapter
returns to selected DocAble incidents where that sequence can actually be reconstructed. They are
not a claim that every model or control in the system arose from failure. Some obligations were
encoded ex ante; some abstractions were clean design choices that simply held. These cases are the
histories where pressure, response, and durable consequence are all visible.

<!-- point: each-episode-asks-five-questions-and-the-last-is-about-capital | Five questions per episode; the last — what cost it retired — makes it capital, not a clever fix. | terms: engineering-capital -->
Each episode uses the same questions. **What happened? What did we try first? Why was that response
insufficient? What became durable? What recurring cost or risk did that asset retire?** The last
question is what makes this a chapter about engineering capital rather than a catalogue of clever
fixes. Sometimes the resulting asset is a constraint or a gate. Sometimes it is a model, a
measurement, a decision rule, or a reusable procedure. Sometimes the evidence does not justify
mechanizing the judgment at all.

## The instruction satisfied to the letter

<!-- point: an-instruction-satisfied-to-the-letter-and-voided-in-spirit | An under-specified instruction is satisfied to the letter and voided in spirit. | terms: stringly-typed, explicitness-is-essential -->
**Problem.** By April the system was 300,000 lines that worked but were janky. It had started as an
MVP built only to prove the thing was possible, so I had imposed real architecture on the parts I
knew were sensitive and hoped the agent would get the rest right. The web layer and the front end
turned crufty.

**Initial response.** I told it to use typed Python. It did, in a sense: it used the type `string`
for everything. Stringly-typed — the word "type" satisfied to the letter and voided in spirit, with
none of the safety the compiler could give. Told to retrofit real types, it turned on the checker
and made everything strings and ints, without building the abstractions I wanted. The same thing
happened porting JavaScript to TypeScript that week. Each port took the cheapest path, because the
agent had no model and did not know the names of things.

<!-- point: why-insufficient-the-rule-named-a-surface-not-the-abstractions | "Use types" named a surface property, not the domain abstractions the types were meant to represent. | terms: explicitness-is-essential -->
**Why insufficient.** "Use types" specified a surface property, not the abstractions the types were
supposed to represent. The agent could satisfy the checker cheaply by annotating primitive
structure rather than discovering the domain structure the instruction was intended to produce. The
problem was not that an agent maliciously gamed the rule. The rule simply did not state the
engineering question at the grain where the desired structure existed.

<!-- point: the-retrofit-combined-cleanup-with-structure | The retrofit combined a commodity-lint floor with a structured model per dense primitive region. | terms: audits-into-lints, make-lint-blocking, dense-primitive-region -->
**What became durable.** The retrofit combined cleanup with structure. I turned on every commodity
lint the language ecosystems ship — hundreds of off-the-shelf smell checks — and for several weeks
burned a stack of coding-agent subscriptions driving each to zero, making it blocking the moment it
hit zero so the smell could never return. Then I worked region by
region. A dense primitive region is a stretch of code doing many low-level operations by hand — raw
library calls, strings and ints passed around untyped, tree-walking spelled out inline — with no
abstraction that owns them. I said "pursue this model" — a clean model-view-controller
decomposition, a reactive web stack friendly to auto-scaling — and reached it iteratively: find a
dense region, induce a typed domain abstraction that owns it, route the region through it, make the
architectural relations explicit, promote the checks as the estate reached conformance. The
important artifact was not the rewritten source. It was the set of representations and controls that
kept the new shape from immediately dissolving.

<!-- point: return-capital-formation-at-brownfield-scale-not-better-lines | This was capital formation at brownfield scale — inherited abstractions, boundaries, and regression checks. | terms: engineering-capital, brownfield-migration -->
**Return.** A month later the 300,000 lines were 200,000, smaller because structure is compression
and the primitive sprawl had been the bloat. This was capital formation at brownfield scale, not
"200,000 better lines." Future work inherited named abstractions, stronger boundaries, and checks
against regression. The repository-motion data in [appendix: appendix-evidence-ledger] is consistent with a concentrated
restructuring episode followed by lower deletion volume, but it does not prove the later stability
was caused by those controls.[^road-to-hell]

[^road-to-hell]: My field notes on the period were less measured: "Claude is the fastest road to
    hell." The point was not the model vendor. It was the speed with which a weakly specified
    direction could turn into a large amount of coherent wrong structure.

## The architecture was the constraint

<!-- point: optimizing-within-an-architecture-that-was-itself-the-constraint | I optimized within an architecture that was itself the constraint. | terms: velocity-exposes-the-danger -->
<!-- index-def: velocity-exposes-the-danger -->
**Problem.** The service cost tens of dollars a day while idle on a Kubernetes cluster.

**Initial response.** The first response stayed inside the existing architecture: tune the
autoscaler, reduce idle pods, and push the cluster toward scale-to-zero. For weeks the agents made
it better and better.

**Why insufficient.** Every improvement ran into the same boundary. Cold pods took too long to
return, so further optimization traded one unacceptable outcome for another — a multi-minute cold
start is a worse problem than the bill. The repeated failure was information about the *frame*
rather than the implementation inside it.

<!-- point: a-recurring-wall-is-the-signal-to-price-a-paradigm-change | A fix that keeps hitting the same wall is the signal to price a paradigm change. | terms: velocity-exposes-the-danger -->
**What became durable.** The durable lesson was a decision rule: when repeated in-frame optimization
collides with the same architectural ceiling, price the paradigm change rather than commissioning
another round of local optimization. The eventual serverless migration encoded the architectural
decision in the system itself, letting the platform own the scaling I had been hand-building against
its own ceiling.

<!-- point: a-judgment-becomes-capital-only-when-later-work-can-inherit-it | A judgment becomes capital only when later work can inherit it — not every lesson in a head is capital. | terms: engineering-capital -->
**Return.** The incident did not initially create a governance mechanism, and I will not pretend that
it did. A judgment becomes engineering capital only when later work can inherit it — through the
resulting architecture, a documented decision rule, a skill, or another durable representation. The
failure produced the insight; externalizing the insight is what turns it into an asset. Not every
lesson in someone's head is engineering capital.

## The decision left open

<!-- point: an-unmade-decision-is-where-the-agent-guesses-worst | The unmade decision is where the agent guesses worst. | terms: optionality-is-poison -->
<!-- index-def: optionality-is-poison -->
**Problem.** My merge-train cron kept stalling when two agents edited the same file.

**Initial response.** It proposed a one-flag fix: on any conflict, take one side and drop the other.
I signed off without asking what "drop the other side" meant.

**Why insufficient.** For four days it silently deleted real work from the main branch every time two
changes overlapped. The same commit heading landed again and again, each time a little smaller,
while I chased the symptoms one missing function at a time. The conflict handler contained a
consequential design choice that had never been made explicitly: what information may be discarded
when two changes overlap? At execution time the agent still had to choose, so it selected the
locally expedient behavior that removed the conflict. The problem was not "optionality" in general.
It was an unresolved consequential decision arriving at the point of autonomous execution.

<!-- point: decide-conflict-handling-up-front-name-the-resolver | Decide conflict handling up front; the resolver reconciles the files, and the destructive default is barred. | terms: optionality-is-poison, explicitness-is-essential -->
**What became durable.** The resolver was changed to inspect and reconcile the conflicting files
rather than selecting one side blindly, and the destructive default was prohibited. The choice no
longer had to be re-guessed on every conflict.

<!-- point: a-decision-made-once-and-encoded-is-engineering-capital | A consequential decision made once and encoded is capital: future work inherits it instead of re-guessing. | terms: engineering-capital, judgment-into-infrastructure -->
**Return.** A consequential decision made once and encoded is a form of engineering capital: future
work inherits the decision instead of paying another probabilistic judgment at the worst possible
moment. The portable lesson is narrower than *optionality is poison*: do not leave consequential
semantics unresolved at the boundary where an autonomous actor must act.

## "Done" was a claim

<!-- point: two-green-reports-that-were-not-true | Two green reports, neither of them true. | terms: done-is-a-claim -->
<!-- index-def: done-is-a-claim -->
**Problem.** Two incidents, close together, made "done is a claim" non-negotiable.

**Initial response.** I trusted the reports. Once, a lint gate hit a stray error, caught it in a
catch-all that returned success, and let a run of commits land completely unlinted while the log
said fine. Another time a chain of pin tests passed green.

**Why insufficient.** A botched merge had landed those pin tests onto a tree missing the very field
they were supposed to check, so they confirmed a field that existed and never touched the one that
mattered. Both reported done. Neither was. These are agent-shaped failures: a report of "done"
describes the producer's intent, not the state of the repository, and the gap between the two is
where a confident green hides.

<!-- point: separate-production-from-admission-and-regenerate-the-evidence | The response separated production from admission: re-run the gates against the real tree; make them fail-loud. | terms: done-is-a-claim, audits-into-lints -->
**What became durable.** The response separated production from admission. Required tests and lints
were re-run against the actual candidate tree at the relevant boundary rather than trusting the
producing agent's report that they had run. Gate failures were made fail-loud: a control that
swallows its own error is worse than no gate, because it reports success it did not earn, so a
control cannot translate its own uncertainty into success. Where useful, counts and structural
evidence were re-derived rather than accepted from the task report.

<!-- point: the-capital-is-machinery-that-regenerates-evidence-not-a-habit-of-distrust | The capital is machinery that regenerates the evidence cheaply — not a standing habit of human distrust. | terms: engineering-capital, judgment-into-infrastructure -->
**Return.** "Done" became a claim requiring independent evidence. The capital is not a standing habit
of human distrust; it is the machinery that lets future work regenerate the relevant evidence
cheaply and consistently. The producer may have authored some of the tests. It does not get to
decide, by report alone, whether the admission boundary should believe them.

## A content-free detector, measured on the wrong world {#fieldnote-content-free-detector}

<!-- point: a-detector-that-measured-the-right-quantity-on-the-wrong-world | A detector measured the right quantity on the wrong world. | terms: tests-for-agent-failure-modes -->
<!-- index-def: tests-for-agent-failure-modes -->
**Problem.** Late in the same stretch, an agent wrote me a content-free detector: if an image's
pixels barely vary, skip the vision model and mark it decorative. Clean, typed, careful.

**Initial response.** Its own comment promised a false positive was "not possible" — real photos and
charts vary twenty times more than the cutoff, so nothing real could slip under. The margin was
true.

**Why insufficient.** It was measured on the wrong world. Hand it a Rothko, or Malevich's black
square, and the pixels barely vary; the detector marks the painting decorative and drops it, and a
blind student reading an art book gets silence where the subject should be. No threshold saves you,
because a blank spacer and a Malevich are identical in the one quantity the code looked at.
"Content-free" was never a fact about pixels. It is a fact about an image in a context of use, and
the use here is teaching, and teaching includes the one tradition built to prove a picture need not
vary. That confident "not possible" was the tell: it marks the spot where the model's picture of its
own inputs was too small.

<!-- point: the-cheap-proxy-loses-the-last-word-to-an-independent-oracle | The cheap proxy loses the last word; ambiguous cases route to a richer judgment and the regression covers the class. | terms: tests-for-agent-failure-modes -->
**What became durable.** The cheap proxy lost the last word. Ambiguous cases are routed to a richer
semantic judgment, and the regression evidence covers the *failure class* — low-variance images that
nevertheless carry content — rather than only the first example that exposed it.

<!-- point: build-validation-around-the-producing-systems-actual-failure-modes | Build validation around the failure modes the producing system actually has: an independent oracle that can falsify the proxy. | terms: tests-for-agent-failure-modes -->
**Return.** Build validation around the failure modes the producing system actually has. A coherent
subsystem can be wrong because its proxy was calibrated on too small a world. The durable defense is
not a "sensor instead of a checkbox"; it is an independent oracle and a regression class capable of
falsifying the proxy's assumption.

## The retry that became a storm

The failures so far were about operating the fleet. This one and the two that follow are about the
product the fleet built — the models inside DocAble — and each, like the mechanisms above, began in
a failure.

<!-- point: automatic-retry-amplified-the-load-it-was-meant-to-relieve | Automatic retry amplified the load exactly when the system was already hurting. | terms: governance-conversion -->
**Problem.** The dominant production failure was a slow generation call, and the lifecycle around it
was implicit. Status got written from scattered places, and one terminal state masked many distinct
internal failures.

**Initial response.** The reflex to any failed job was to requeue it. Retry was the default recovery
for every timeout and every crash.

**Why insufficient.** Under a slow dependency that reflex became a storm — requeue, still slow, same
timeout, requeue again — and the recovery amplified the load precisely when the system was already
hurting. Cost hid the damage. Every attempt wrote its full cost with nothing marking it a retry, so
a job tried three times billed roughly three times over and the dashboards showed a silent
multiplication no one could explain.

<!-- point: an-explicit-lifecycle-an-atomic-claim-and-a-cut-retry | An explicit lifecycle, an at-most-once compare-and-set claim, and no automatic requeue replaced the reflex. | terms: governance-conversion -->
**What became durable.** Three structures replaced the reflex. An explicit lifecycle named the states
and the legal transitions and put one guarded function in front of them that refuses any illegal
move. An atomic compare-and-set let a chunk be claimed only from the queued state, so redelivery
could not grant two workers permission to perform the same work concurrently; the at-most-once
permission rests on that claim, not on a lease that can expire — the lease governs recovery, not the
right to act. Finally, ordinary failure stopped implying automatic requeue: a failure now surfaces
as failed, the one sanctioned exception being planned preemption, a deliberately modeled recycle
mid-flight rather than a failure, with a check that keeps the exception from widening back into
general retry.

<!-- point: a-later-sweep-learned-that-slow-is-not-dead | A later stale-owner sweep exposed a missing distinction — slow is not dead — and progress evidence supplied it. | terms: governance-conversion, judgment-into-infrastructure -->
**Return.** A later stale-owner sweep exposed another missing distinction: slow is not dead. The
sweep began reaping jobs that were slow but still advancing, so progress evidence was added and
recovery could then tell abandoned work from work still advancing. The accumulated capital was a
better behavioral model plus safer recovery authority. Recovery stopped being a reflex and became a
decision over explicit state and evidence.[^registry-day]

[^registry-day]: A neighboring concurrency failure taught the same lesson from the other side. Two
    workers that a semaphore was rationing "safely" still corrupted a shared registry file — the
    semaphore rationed contention and guarded nothing that decided who writes. It cost me a day of
    chasing the corrupted file before I saw that the two rationed writers were the whole problem.

## Provenance became a lookup

<!-- point: the-simplest-forensic-question-was-expensive | The system could not cheaply answer which step wrote a bad element. | terms: governance-conversion -->
**Problem.** A corrupted document once took four cross-file hops to trace back to the pass that wrote
it. Each hop was cheap; the chain was expensive. The system could not cheaply answer the plainest
question a remediation tool owes its user: which step changed this?

**Initial response.** Stamps were written by hand, site by site, with nothing checking that a given
mutation was stamped at all. A low-level write path recorded its changes into the document's own
structure and never into the registry the changelog reads.

<!-- point: bypasses-produced-empty-changelog-rows | Eleven sites bypassed the stamp helper and shipped empty changelog rows for a whole class of jobs. | terms: governance-conversion, done-is-a-claim -->
**Why insufficient.** Verification found eleven production sites writing through that low-level path,
skipping the stamp helper, shipping empty changelog rows for a whole class of jobs. The customer's
account of what changed came out blank while the code had changed plenty. An empty changelog row is
"done" as a claim again — a report the work cannot back.

<!-- point: the-changelog-became-a-projection-of-stamps | Stamps became mandatory and the changelog a derived projection of them, the wiring check landed warn-then-block. | terms: governance-conversion -->
**What became durable.** The changelog stopped being an independently authored account. Each mutation
writes its stamp — the verb, the pass, the visibility — and the customer-facing changelog is derived
from those records rather than authored by hand, so diagnosis stopped being inference and became a
lookup. A wiring validator identifies mutating paths that fail to stamp their work. It reported
rather than blocked at first, because the inherited tree still contained many violations; a fix wave
drained the gap; only then did the check receive admission authority.

<!-- point: audit-drain-promote-is-the-reusable-move | Representation made the obligation observable; migration made it feasible; gating made it authoritative. | terms: governance-conversion -->
**Return.** A forensic question that once required several cross-file hops became a lookup over
derived evidence — bounded by the stamp model and the wiring check, not a guarantee larger than
those. This is governance conversion in a particularly visible form, but even here the sequence
matters: representation made the obligation observable; migration made it feasible to satisfy;
gating made it authoritative. Audit → Drain → Promote is the reusable move.

## Measurement: the story that stops before the gate

<!-- point: a-timeout-spoke-only-after-the-budget-was-spent | A silent hang failed after eight minutes with an opaque message and a progress bar frozen at thirty-eight percent. | terms: governance-conversion -->
**Problem.** A document's repair stalled in silence. The subprocess was killed by the job timeout
after roughly eight minutes, and the job failed with an opaque message while the customer's progress
bar sat frozen at thirty-eight percent, the budget already spent.

**Initial response.** The timeout was a bare cap. It provided no predictive account of per-chunk time
or cost and no useful baseline for deciding whether the current work was approaching the job
ceiling.

<!-- point: the-incident-created-the-model-and-its-calibration | The incident forced a provisional per-chunk worst-case model against the job budget, seeded and kept as external data. | terms: governance-conversion -->
**What became durable.** The incident produced a provisional measurement and a per-chunk worst-case
model against the job budget. The seed remains externalized, timestamped, and explicitly provisional
— kept as a datapoint in a file rather than frozen as a constant in the code — so one incident does
not quietly become an eternal constant. Current instrumentation can report a would-be breach.
[ref:measurement-no-gate] draws where the chain deliberately stops.

<!-- label: measurement-no-gate -->
<!-- figure: assets/measurement-no-gate.svg | *Modeled and Observed, Not Yet Binding.* A bare timeout motivates a measurement, which seeds a per-chunk cost-and-time model, whose evidence lets the environment report a predicted breach. The chain stops there: production admission deliberately does not depend on it, and the terminal authority node is left open. The open node is part of the evidence — representation and observation can mature before authority is justified. An engineering history, not a system model. -->

<!-- point: the-history-stops-before-the-gate-on-purpose | The history stops before the gate on purpose: the bound is not calibrated strongly enough to deserve authority. | terms: governance-conversion -->
**Return.** The history deliberately stops there. No production gate rejects the job on this model
today. The bound is not calibrated strongly enough to deserve that authority, and a false refusal
could cost more than continued observation. Better representation and better evidence are already
useful engineering outcomes. **Alignment need not culminate in a blocking gate.** The question mark
belongs in the book.

<!-- point: measurement-earns-authority-when-the-number-is-deterministic | The dual case: a deterministic cold-start number DID earn authority, feeding a topology model that drove a re-architecture from 4,057 ms to 109 ms. | terms: engineering-capital -->
The counterpart shows measurement earning authority when the number deserves it. A serverless fleet
that scaled to zero still carried a warm per-request floor that a fleet-average number hid. Measured
deterministically at the request level, that floor read as 4,057 ms; fed into a deployment-topology
model that summed the longest cold-start path, it named the worst latency a real user could hit and
drove a re-architecture that cut the floor to 109 ms. Here the number fed the model, and the model
was calibrated enough to drive a change rather than merely report one. The two cases bound the same
principle from opposite sides: measurement becomes capital, and whether that capital earns
enforcement authority depends on whether its bound can bear the weight.

<!-- point: a-null-result-can-be-honest-only-at-the-level-the-effect-lives | A null-result ablation found no effect only because both arms scored at the ceiling — measure the effect where it lives. | terms: engineering-capital -->
Measurement honesty runs the other direction too. One ablation on the doc-derived tests found no
effect — but only because both arms already scored at the ceiling, leaving no headroom for a
difference to appear. The null result was a fact about the measurement, not the intervention: it
taught the discipline of measuring an effect at the level where it actually lives. That discipline
is what makes the other numbers in this case trustworthy.

<!-- point: evidence-is-honest-about-which-rung-it-reaches | Deployed, cleared checkers, and measured fidelity are three different rungs, and none is end-user access. | terms: field-report -->
And measurement is not the same as safety. DocAble was deployed, it cleared the institutional
checkers a document is required to pass, and it measured its own fidelity — but none of those three
is the same as a blind student finding the document usable. This Part is the evidence, and the
evidence is honest about which rung it reaches.

## A conversion that was almost mechanical

<!-- point: some-conversions-were-almost-mechanical | Some conversions were almost embarrassingly mechanical: a repeated review finding became a blocking lint. | terms: governance-conversion, audits-into-lints -->
Some conversions were almost embarrassingly mechanical. A review kept finding the same decidable
violation in the repository; the response was not to remind future agents more forcefully, but to
turn the audit question into a blocking lint. The judgment moved from repeated inspection into the
environment, where it fired the same way on every commit.

One case is small enough to state in a sentence. Invisible remediation artifacts — the ones a
document gains for accessibility but an author never sees — were supposed to carry a reserved name
prefix so a validator could always tell an inserted element from an authored one. Review kept
catching insertions that skipped it. A prefix rule is entirely decidable, so the check became a
lint: any inserted artifact without the prefix now fails the build rather than waiting for a
reviewer to notice it was missing.

## Not every useful structure has an incident arc

<!-- point: one-model-was-confirmed-not-strengthened | Some structure was designed cleanly from known obligations and simply held — reported as confirmation, not conversion. | terms: governance-conversion -->
The chapter would be less credible if every piece of engineering followed the same five-beat story.
It did not. The unified detect-and-repair engine — one rule engine that ended a duplication tax
where two separate checkers had scanned the same document in two languages a round-trip apart —
consolidated those paths for a clean architectural reason. A later audit, prompted by a routing
incident elsewhere, tested the boundary and found it holding. No subsequent failure forced the model
into a stronger form.

<!-- point: the-case-contains-both-ex-post-and-ex-ante-paths | The originating case contains both paths: some assets forged in incidents, others designed from known obligations and simply survived. | terms: governance-conversion -->
Report that as confirmation, not conversion. Some assets in DocAble were forged by incidents and
hardened through recurrence; others were designed from known obligations and simply survived later
pressure. Part III called these the ex-post and ex-ante paths into the governed environment. The
originating case contains both, and reporting the difference is what makes the rest of this
archaeology worth trusting.

## What this case supports

<!-- point: agentic-velocity-amplifies-the-surrounding-engineering-judgment-both-ways | Agentic velocity amplified the quality of the surrounding engineering judgment in both directions. | terms: judgment-into-infrastructure -->
Step back from the individual incidents and one pattern does recur. Agentic velocity amplified the
quality of the surrounding engineering judgment in both directions. An underspecified choice could
become a large coherent implementation quickly; a well-chosen abstraction or invariant could
likewise propagate quickly once it was made durable. The interesting unit was therefore not the
agent's isolated success or failure. It was what the engineering environment learned from the
episode and whether later work inherited that learning.

<!-- point: the-cases-show-several-forms-of-inheritance | The cases show several forms of inheritance — typed seam, deterministic check, behavioral model, derived provenance, provisional model — some gated, some not. | terms: engineering-capital -->
The cases above show several forms of that inheritance. An architectural decision became a typed
seam; recurring review findings became deterministic checks; an implicit lifecycle became an
explicit behavioral model; expensive forensic reconstruction became derived provenance; an opaque
timeout became measurement and a provisional predictive model. Some outcomes received authority.
Some deliberately did not. Some useful structures were not incident-driven at all. That variation is
the evidence, because it is what keeps governance conversion from becoming a formula applied
retrospectively to every event.

<!-- point: the-claim-is-bounded-one-engineer-one-ecosystem-no-controlled-comparison | The claim is bounded: one longitudinal case, one primary engineer directing a fleet, one model ecosystem, no controlled comparison. | terms: field-report -->
The resulting claim is bounded. This is one longitudinal production case led by one primary engineer
directing an agent fleet, using one contemporary model ecosystem, without a controlled comparison
against another engineering process — one codebase and one pass through it, not a migration run
twice with and without governance. It cannot establish that another organization will encounter the
same failures, build the same mechanisms, or achieve the same economics. Nor do checker scores and
fidelity instrumentation establish the complete experience of a blind student using every remediated
artifact.

<!-- point: what-the-case-can-establish-is-mechanism-and-sequence | What the case can establish is mechanism and sequence: these pressures, these responses, these structures that persisted under change. | terms: field-report, engineering-capital -->
What the case *can* establish is mechanism and sequence. These pressures occurred; these engineering
responses followed; later failures sometimes exposed residual gaps; and selected structures
persisted long enough to be exercised under continuing change. In that sense the clean MAGE method
is not the story the project followed. It is the compression recovered from the engineering
structures that repeatedly proved worth keeping.

<!-- point: part-vi-changes-the-evidentiary-question | Part VI leaves the originating case and asks whether independently built systems expose the same pressures and moves. | terms: field-report -->
Part VI changes the evidentiary question. It leaves the originating case and asks whether
independently built systems expose the same pressures, make comparable moves, or reveal boundaries
the DocAble case could not.
